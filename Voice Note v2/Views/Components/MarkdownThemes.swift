import SwiftUI
import MarkdownUI

// TEMPORARY FILE: Minimal implementation to fix build
// Remove this file from Xcode project and delete it after build works

extension Theme {
    static func voiceNoteDefault(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func keyQuotes(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func actionList(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func ideaOutline(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func smartSummary(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func nextQuestions(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func brainstorm(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func flashcards(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func toneAnalysis(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
    
    static func cleanup(colorScheme: ColorScheme) -> Theme {
        Theme()
    }
}